# 13 — 功能流程與進度（總覽）

> 一頁看懂全貌：住戶/物業流程 + 各功能執行狀態 + 核心閉環依賴。
> 決策見 [10-規則決策總表.md](10-規則決策總表.md)（Q1–Q16）；開發 TODO 見 [../TODO.md](../TODO.md)。
> 圖例：✅ 完成　🔄 處理中　⬜ 未做(stub)　🔴 等管委會。最後更新：2026-06-24。

---

## 住戶流程

- ✅ 看說明 / 管理辦法 — `rules.html`
- ✅ 線上登記（或紙本，物業代登）— `RegisterView` + Supabase（已登入點「登記」自動進編輯）
- ✅ 選位填志願 — `SelectView`（地圖排志願 → **已存 Supabase**）
- ✅ 登入查結果 — `MeView`：登入/重整保持登入 ✅；**抽籤結果回填**（車位/應繳/簽約期限/未中候補）✅（物業發佈後即可查）
- ⬜ 簽約繳費（Phase II）

## 物業流程（核心閉環：登記 → 抽籤 → 結果 CSV 已端到端可跑）

> 管理員登入：戶號 `admin` ＋ 車號 `樂菲莊園`（前端特例 → `is_admin`；nav 出現「🔧 管理」）。
> 維護頁 `/seat-admin` 與 dev 專區 `/dev` 限**管理員**進入；配位頁 `/allocate` **公開**（預設跑範例 demo，鄰居可玩、看演算法）。真實名冊匯入只在 dev 專區（個資不進公開頁）。

- ✅ 收登記（線上 + 紙本併冊、去重、衝突列出）— `registry`
- ✅ **匯出登記名冊** — `scripts/export-roster.mjs`（service_role 本機撈全戶 → `private/roster-*.csv`，gitignored）或後台 SQL Download CSV（見 doc 14）
- ✅ **車位指派維護** — `SeatAdminView`：地圖點位 → 填車號/車種/類型/**繳費** → 指派並鎖定（保留/志願小位/無障礙/維修）；抽籤排除、選位不可選、**跨裝置**。安全分流：車號/繳費寫 `vehicle`(deny-all)、id 寫 `locked_seat`(公開)
- ✅ **跑抽籤/分發** — `AllocateView`（**公開**，預設 20 戶範例 demo）：設種子 → `distribute` → 結果(順序號連續排序)/免抽分流/落選/監察 log/**重抽** → **⬇ 下載配位結果 CSV** → **📢 發佈**(限管理員)。**真實名冊匯入**移到 dev 專區（抽籤日匯入 → 共用 store → 回此頁大螢幕現場抽）
- ✅ 公告 / 結果輸出 — **公告走「⬇ 下載配位結果 CSV（Excel 開）」**(AllocateView)；住戶線上查走 MeView。**`ExportView` 已刪除**（2026-06-24：檔案/路由/nav「匯出」/首頁 step4 一併移除；eTag 白名單匯出移入 Phase II）
- ⬜ 收繳費(大位中籤) / 候補遞補 — `ResultView`（Phase II；小位/無障礙的繳費已可在指派維護登打）

---

## 執行概況（依狀態）

### ✅ 完成 / 上線

- M0 骨架、登記(+編輯)、說明頁、座位盤點 654（大341/小305/無8；公益/漏水旗標）
- 分發引擎 `distribute.js`（填志願統一分發、固定 seed 可重現、+測試）
- **抽籤配位頁 `AllocateView`**：匯入名冊 CSV、設種子、重抽、監察 log、結果/落選表
- **志願上雲**：household 加 `車位志願 text[]`/`志願落選保底`（migration 0003）+ `save-wishes` Edge Function + `SelectView` 接雲端 + login 帶回
- **登記名冊匯出** `scripts/export-roster.mjs`（service_role 本機 → CSV 橋）；**抽完下載配位結果 CSV**（`result.js`，公告日→簽約期限）
- **順序號跨輪連續累加**（每車唯一、不共用）；配位結果依順序號排序；**免抽(小位/無障礙)分流顯示**；落選名單含順序號
- **共用地圖元件 `<SeatMap>`**（選位頁 / 維護頁共用：pan/zoom/聚焦）
- **車位鎖定上雲**：`locked_seat` 表（anon 可讀）+ `set-locked` Edge Function（驗 `ADMIN_PASSWORD`）+ `store/locked.js`（reactive，三頁讀雲端）→ **跨裝置鎖定**
- **車位指派維護（含繳費）**：維護頁由「純鎖定」升級為「指派＋鎖定」（migration 0006 vehicle 加`已繳費`；`assign-seat` Edge Function list/assign/unlock）。**安全分流**：戶號/車號/繳費 → `vehicle`(RLS deny-all)；車位 id → `locked_seat`(公開)。支援 11/1-15 先確定志願小位/無障礙+繳費
- **管理員特例登入**（admin/樂菲莊園 → is_admin）＋ 路由守衛（/seat-admin、/allocate 限管理員或 dev）＋ nav「🔧 管理」
- **session 持久化**（sessionStorage：重整保持登入、關分頁清）
- Supabase 後端 **7 支 Edge Function**：register / login / update-household / save-wishes / set-locked / publish-result / assign-seat；GitHub Pages env 注入（已實機核對：repo 7 支＝線上 7 支、migrations 0001–0006）
- **mock 工程師專區**（dev-only **且限管理員**：灌 20 跨棟戶 / 清除測試資料(SQL) / **清除配位結果**(送空 rows，免 SQL) / 分發 demo / **匯入真實名冊 CSV → 共用 store → 配位頁抽籤**；`import.meta.env.DEV` + isAdmin 雙守門）
- 測試 94 全綠

### 🧪 下次待測（已做、未實機驗證）

- **順序號連續累加**：實機重跑 `/allocate` 確認 1..N 連續、每車唯一、落選號在落選表。
- **一人多車位**：同戶第1/2/3輛在 R1/R2/R3 各拿一個順序號、分到不同位、結果頁正確顯示。
- **結果回填**：管理員發佈後，住戶登入 `/me` 看到自己車位/應繳/期限（跨裝置）。

### 🔄 / ⬜ 剩下

- ✅ **結果回填 `MeView`** 已做 — 抽籤結果寫回 Supabase（publish-result）、login 帶回、MeView 顯示。已實機驗後端正常(空 rows 打 publish-result 回 200)。⚠️ **已知 latent bug**：publish-result 的 `updated` 計數會**虛報**——`.update().eq()` 配 0 列在 PostgREST 不算錯，所以數的是「名冊筆數」非「實際命中」；若匯入名冊車號未在 Supabase 登記，會顯示「已發佈 N 筆」但住戶查無（疑似「發佈沒反應」的根因）。建議改用 `.select()` 回實際命中數、並把查無車號列出
- ✅ **無障礙／小位物業指派** 已做 — 維護頁可「指派車位給戶（車號→位→繳費）」，寫 vehicle + 鎖定該位 → 移出抽籤。distribute 對齊已完成（見下）
- ✅ **distribute 與物業指派對齊**（2026-06-24）：名冊匯出多帶 `車位編號`+`已繳費`；distribute 對「已有車位編號」者**跳過抽籤、直接記入結果**（型別反查主檔；重機＝兩位同車號→占2位）；結果狀態分「**已繳**(指派)／**分配**(中籤待繳)」。改 export-roster/registry/distribute/result/AllocateView，+5 測試（共 102 綠）
- **管理員升級為後端 `is_admin`**：目前是前端特例（admin/樂菲莊園 在 bundle）。⚠️ `樂菲莊園` 在前端有**兩個用途**——① `tryAdminLogin` 登入特例 ② `ADMIN_PASSWORD` 寫入密碼（送 set-locked/publish-result/assign-seat 驗）。**只把①搬進 DB 沒用**：②仍在 bundle，公開字串照樣外露。要讓它真正消失＝加 `household.is_admin` 欄 + 寫入函式改驗「認證車號→is_admin」（同 update-household 擁有權證明）+ 管理員改用**住戶私有車牌**（非公開的 admin/樂菲莊園）。另：安全掃描已確認 service_role/anon key/DB 密碼皆未進 repo、`.env` 已 gitignored（⬜）
- ~~**`ExportView`**~~ **已刪除**（2026-06-24）：公告走 CSV/Excel 下載(AllocateView)、住戶查走 MeView；檔案/路由/nav「匯出」/首頁 step4 全移除（`src/export/result.js` 保留＝結果 CSV 模組，與 ExportView 無關）
- `ResultView` 繳費維護 / `PatrolView` 巡邏（Phase II）

### 🔴 等管委會（見 doc 10 Q1–Q16）

- Q1 候補、Q2 用語落差、**Q16 時間線分兩階段**（合規必決）
- Q5 收款管道、Q6 重機區、Q7 公益位、Q10 498、Q11 大小分型、Q12 腳踏車、Q13 動線/轉角、Q14 換位收費、**Q15 工作人員剩餘位（繳費/登記時點/數量/順序）**

---

## 核心閉環依賴鏈（登記 → 抽籤 已打通）

1. 登記 ✅
2. → 選位志願（存 Supabase）✅
3. → **匯出名冊** `export-roster.mjs` → CSV ✅
4. → **匯入 AllocateView** → 跑 `distribute` ✅
5. → 結果：**物業下載配位結果 CSV ✅（AllocateView）** + **回填 `MeView`（住戶線上看自己）✅（publish-result，見上 ⚠️ updated 虛報 latent bug）**
6. → 繳費 / 候補（Phase II）

> 1–5（物業端）已端到端可跑：匯出名冊 → 匯入抽籤 → distribute → 下載結果 CSV（橋接有測試鎖定）。
> 另：物業可先在 `/seat-admin` 鎖定保留位（上雲、跨裝置）→ 抽籤自動排除。
> **核心閉環已通（含物業指派對齊）；剩餘：publish 計數修正(#2)、落選第二階段(#3/Q1)、Phase II 繳費/候補。**

## 三介面分工（避免重疊）

- `MeView` ＝ 住戶看**自己**（登入、唯讀）
- ~~`ExportView`~~ ＝ **已刪除**：物業看全部 + 下載由 AllocateView 取代；公告走 Excel、住戶查走 MeView
- `ResultView` ＝ **Phase II** 繳費狀態維護 + 候補遞補

## Phase II（後置）

- 繳費狀態旗標 + 退費試算 + 候補遞補閉環
- 換位窗口（Q14：是否收 100 手續費）
- **eTag 白名單匯出**（車號→可通行 CSV，餵閘門系統；原 ExportView 唯一未做項，刪頁後改記於此）
- 巡邏稽核 + 車牌辨識 + 勸導單
- 登記掃描帶車號（LPR）、年度歷史
