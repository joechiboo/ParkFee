# 全鏈實測計畫（2026-09-01）

> 原訂 8 月底、已逾期。本輪同時驗收 9/01 新增的三項功能。
> 分工：**A 段自動**（引擎／API，Claude 跑）、**B 段手動**（瀏覽器 UI，使用者跑）。

## 本輪必看的新功能（8/27–9/01 新增，皆未經真實流程）

| # | 功能 | 驗什麼 |
|---|---|---|
| N1 | 重機**實體相鄰**配對 | 配到的兩格真的相連，無隔走道／轉角（原 71 對假相鄰） |
| N2 | `/allocate`「⬇ 載入真實名冊」 | 能載入、且**電話欄為空**（個資最小化） |
| N3 | 工作人員選位限制 | 公益位與無障礙位點不下去 |

---

## A 段：自動（Claude 執行）

| 編號 | 項目 | 判定 |
|---|---|---|
| A1 | 單元測試 196 ＋ `vite build` | ✅ 全綠、build 成功 |
| A2 | 引擎全鏈：真實鎖定格（44）＋410 列名冊 | ✅ 重複配位 0、鎖定位外洩 0、同戶第1輛重複 0 |
| A3 | 重機實體相鄰（N1） | ✅ 18 台（雙格 16＋無障礙單格 2），假相鄰 **0** |
| A4 | 健檢腳本 13 項 | ✅ 全 13 項正確觸發（需處理 8＋參考 5） |
| A5 | API 負面路徑 | ✅ 六支 403；三張敏感表 REST 直讀回 `42501 permission denied` |
| A6 | CSV round-trip ＋ 戶層級旗標 | ✅ 旗標不失真、戶層級不一致 **0 戶** |

**A 段結論（2026-09-01 執行）：全數通過。** 明細：
- 名冊 410 列 → 配出 175、落選 159；剩餘可配 420 格
- 工作人員：配出 0、暫緩 2（因尚有住戶未配到）→ 符合辦法伍二（十二）；公開版正確隱藏 2 筆
- 社宅：配出 11 筆，**無一配到非公益位**
- 落選結構：未填志願 155｜志願全落選 2｜工作人員暫緩 2
  > ⚠️ 155 筆「未填志願」是 mock 名冊特性（僅 4 列有志願），非缺陷。
  > 但它印證了電腦選號預設打勾的必要——場上還剩 420 格卻有人配不到。

## B 段：手動測試流程（瀏覽器）

> 站台 <https://joechiboo.github.io/ParkFee/>（或本機 `http://localhost:5173/ParkFee/`）
> ⚠️ **兩者連同一個 Supabase**，沒有測試庫 → 一律用下列測試身分，測完照階段 7 清除。

### 測試身分一覽（照這個填，清除 SQL 才掃得到）

| 身分 | 戶號 | 車號 | 用途 |
|---|---|---|---|
| 一般戶·重機 | `S1-991` | `TEST-001` | 驗重機相鄰兩格 |
| 社宅戶 | `S1-992` | `TEST-002` | 驗只能選公益位 |
| 工作人員 | 填「測試員」→ 存成 `員工-測試員` | `TEST-003` | 驗前綴自動補、選位限制、配位排最後 |

---

### 階段 1 · 一般戶登記（重機）

1. 開站台 → 確認左上角是「**樂菲莊園**」（不是 ParkFee）＝跑的是新版
2. 進「登記」→ 戶號 `S1-991`、電話 `0912345678`
3. 車輛第 1 台：車號 `TEST-001`、車種選「**重機（250CC 以上）**」
4. 送出

**預期**：出現綠色「✓ 登記完成」，列出「第 1 台 · TEST-001 · 重機」。
**不對的話**：若顯示「請檢查網路」→ 是錯誤訊息被吞（8/27 修過，回報我）。

### 階段 2 · 排志願（驗電腦選號預設）

5. 按「🗺️ 下一步：選車位志願序」
6. **先看右側「電腦選號」是否已打勾** ← 新預設（9/01 改）
7. 在地圖上點 3–5 格（點的順序＝志願序）→ 按「儲存志願序」

**預期**：① 電腦選號**預設打勾** ② 沒有多餘的 ☑ 符號 ③ 存檔顯示「✓ 已儲存到後端」。

### 階段 3 · 社宅戶（驗公益位限定）

8. 回「登記」（或先在 `/me` 登出）→ 戶號 `S1-992`、車號 `TEST-002`、車種一般
9. **勾「社會住宅住戶」** → 送出 → 進選位頁

**預期**：地圖上**只有金圈公益位可以點**（1–7、92–104），其餘一般位點不動；
圖例「可選」只列公益位。

### 階段 4 · 工作人員（驗前綴＋選位限制）★ 新功能

10. 回「登記」→ **先勾「社區工作人員」**
11. 觀察戶號欄提示是否變成「直接打姓名即可，例：陳大明」
12. 戶號欄**只打「測試員」**（不要打員工-）、車號 `TEST-003`、車種一般 → 送出

**預期**：完成頁顯示戶號為「**員工-測試員**」＝前綴自動補上。

13. 進選位頁

**預期**：① **金圈公益位點不下去** ② **無障礙位點不下去** ③ 圖例「不可選」列出
「公益（社宅戶專用）」與「無障礙（保留給行動不便住戶）」 ④ 頁首有橘字說明
「配位排全體住戶之後、住戶未配完時暫緩」。

### 階段 5 · 管理員載入名冊

14. `/me` 登出 → 以戶號 `admin`、**車號欄填管理員密碼**登入
15. 確認導覽列出現管理入口（🔧 管理／配位／🛠 dev）

**5a · 正案路徑（Dashboard → CSV → 匯入）**

16. Supabase Dashboard → SQL Editor → 貼 [docs/14 附錄](14-抽籤作業手冊.md)的 SQL → 執行 → 右上 **Download CSV**
17. 站台 `/dev` →「④ 匯入真實名冊」→ 選剛下載的檔

**預期**：`/allocate` 頂端顯示「**✓ 已載入…**」且**戶數與 Dashboard 查出的一致**。

**5b · 備案路徑（一鍵載入）★ 新功能**

18. `/dev` →「清除匯入（回範例）」→ 回 `/allocate`
19. 按藍色「**⬇ 載入真實名冊**」

**預期**：頂端變成「✓ 已載入…Supabase 即時名冊 · N 戶 / M 台」，**戶數與 5a 相同**。

## 🔁 下週複測計畫（2026-09-2x，約 1 小時）

> **不從頭開始。** 已登打的資料維持現狀（`H3-6` 等真實登記、`S1-991`／`S1-992`／
> `員工-測試員` 三個測試身分都留著），只補「還沒驗過的」與「確認沒壞的」。
> 測試資料統一在 **11 月抽籤前**一次清除（見下方「11 月清場」）。

### 測試身分總表（累積使用，11 月前不清）

> 2026-09-18 定：**原有三戶留著驗證用，缺的路徑補建新帳號**。
> 戶號一律 `S1-99x`（三位數，與 mock 的 `S1-9x` 不撞號），工作人員一律 `員工-測試*`。
> 車號 `TEST-0xx`（`車號` 是 vehicle 主鍵，撞到真實車牌會寫不進去）。

| 戶號 | 車輛 | 勾什麼 | 驗哪條路徑 | 狀態 |
|---|---|---|---|---|
| `S1-991` | `TEST-001` 重機 | — | 重機相鄰兩格、志願序不被無障礙插隊 | ✅ 9/18 已驗（207、208） |
| `S1-992` | `TEST-002` 一般 | 社宅 | 社宅限公益位 | ✅ 9/18 已驗（94） |
| `員工-測試員` | `TEST-003` 一般 | 工作人員 | 排最後、暫緩、公告隱藏 | ✅ 暫緩已驗；**1,200 元未驗** |
| `S1-993` | `TEST-004` 一般＋`TEST-005` 重機 | — | ★ 一戶多車混車種、共用志願互相排擠 | ⬜ 待建 |
| `S1-994` | `TEST-006/007/008` 三台一般 | — | R1→R2→R3 三輪、一戶一位保障 | ⬜ 待建 |
| `S1-995` | `TEST-009` 一般 | **身障** | ★★ **R0 無障礙輪**——至今從未有真實帳號走過（9/18 log：「R0 登記 0」） | ⬜ 待建 |
| `S1-996` | `TEST-010` 一般 | **志願小位** | ★★ **免抽、依登記序選小位**——同樣從未走過（辦法伍二（五）） | ⬜ 待建 |
| `S1-997` | `TEST-011` 一般 | 社宅 ＋ **身障** | 記錄在案的例外：**無障礙位不分社宅身分**（社宅身障者仍可循無障礙優先） | ⬜ 待建 |
| `S1-998` | `TEST-012` 重機 | 社宅 | 社宅重機＝吃 **2 格公益位** | ⬜ 待建 |
| `S1-999` | 自行車 1 台 | — | 純自行車戶：無車牌、**電話必填**、不能線上登入、免費、不排志願 | ⬜ 待建 |

**另外用「屬性」而非新帳號覆蓋的**：
· **電腦選號關閉** —— 於 `S1-994` 第 3 輛所在戶取消勾選，驗「志願落空且未勾＝落選候補」
（9/1 模擬顯示這是落選主因：183 台裡 84 台因此配不到，而場上還剩 507 格）
· **志願指到鎖定位／一般戶指公益位** —— 於 `S1-993` 志願中各塞一格，驗引擎是否正確跳過

> 建帳號時請一併存志願（否則會落到「未填志願」路徑，驗不到志願相關邏輯）。
> ⚠️ 帳號愈多、名冊愈接近真實規模，抽籤結果也愈有參考價值——但**11 月抽籤前務必全數清除**，見 TODO 清場條目。

---

### A. 回歸煙霧測試（約 15 分）— 確認這週的改動沒弄壞既有行為

| # | 做什麼 | 預期 |
|---|---|---|
| A1 | 我先跑 `npx vitest run` ＋ 引擎層 headless | 全綠 |
| A2 | Dashboard「01 匯出登記名冊」→ `/dev` 匯入 | 戶數與當時 Dashboard 列數一致 |
| A3 | **種子沿用 `手動測試-0918` 重跑** | **結果必須與 9/18 逐格相同** |

> ⚠️ **A3 是本段最有價值的一項**：同一份名冊 ＋ 同一個種子必須得到同樣結果——
> 這是抽籤可稽核的基礎、也是對住戶的承諾。若名冊未變而結果變了，代表引擎被動到，
> 要先查清楚才能往下。（若期間有人新登記，名冊會變，此項改為「重跑兩次結果一致」。）

### B. 補齊沒驗過的（約 30 分）

**B1 · 補建測試身分（見上方總表 ⬜ 待建七戶）並跑一輪**

兩戶多車的志願都填**同一組連號區**（如 `230–234`），才看得出互相排擠。核對：第 1 輛 R1 拿單格 → 重機 R2 才輪到、需從**剩下的**志願湊相鄰兩格；
`S1-994` 三台分落 R1/R2/R3 且不重複佔位；R1 跑完前沒有任何一戶拿到第二個位。

**新路徑的核對點（這輪第一次跑到）**

| 戶號 | 預期 | log 應出現 |
|---|---|---|
| `S1-995` 身障 | 配到無障礙位（105–108 一帶），**不佔順序號** | `R0 無障礙：登記 1…` 不再是 0 |
| `S1-996` 志願小位 | **免抽**、依登記序直接取小位 | R1 的「志願小位免抽」段 |
| `S1-997` 社宅＋身障 | 走 R0 拿**一般無障礙位**（非公益位）——這是刻意的例外，不是違規 | 同 R0 |
| `S1-998` 社宅重機 | **2 格公益位**（公益大位 2、3、6、7、97… 之相鄰對） | R1 |
| `S1-999` 純自行車 | 自行車輪隨機配位、**應繳 0 元**；**不能用線上登入查**（無車牌） | 自行車段 |

> ⚠️ `S1-995`／`S1-997` 同時搶無障礙位，而無障礙僅 8 格且已鎖 2 格 →
> 若兩者都中，正好也驗到「R0 登記數 ≤ 可用」的全配路徑；要驗抽籤路徑得再多建幾戶。

**B2 · 工作人員的 1,200 元（9/9 修正，真實流程仍未驗）**

9/18 那輪因工作人員被暫緩而驗不到（落選列 `應繳金額` 本就留空）。
作法：把落選那幾戶的**電腦選號打開**（或補足志願），讓住戶全數配到位 →
工作人員才輪得到 → 確認內部版 CSV 的 `員工-測試員` **應繳金額＝1,200，不是 0**。
> 這條原本的 bug 是「匯出 0 元 → 社區幫出帳整批漏收」，值得走一次真流程。

**B3 · 稽核四動作補齊**（9/18 只驗到 `result.publish`）

| 動作 | 怎麼觸發 |
|---|---|
| `roster.list` | `/allocate` 按「⬇ 載入真實名冊」（備案路徑） |
| `seat.assign` | 維護頁指派任一格給測試車 |
| `seat.unlock` | 同頁解除該格 |
| `result.publish` | 發佈結果（已驗過） |

查：`select at, action, actor, target, ok, detail from public.audit_log order by at desc limit 20;`
四種 action 都要出現，且 `detail` **不得含電話姓名**（只該有筆數與車位/車號）。

### C. 收尾（約 10 分）

`/dev` →「②½ 清除配位結果」還原；測試資料**不清**（留到 11 月）。

---

> ## ⏸ 進度書籤（2026-09-18 收工）
>
> **本輪（9/18）已完成**：階段 1–7 主線全通過，於**正式站台**跑真實資料。
> · 名冊：Dashboard「01 匯出登記名冊」→ `/dev` 匯入＝**21 戶／33 輛**（9/2 是 25/37，中間清過測試資料）
> · 抽籤：種子 `手動測試-0918`
> · **`S1-991` 重機 → `207、208`（大、小）** ← 9/2 修的「志願序不得被無障礙插隊」**在正式環境確認生效**（修正前會拿到第 5 志願的 197）
> · `S1-992` 社宅 → 94（公益小位）✅　·　`員工-測試員` → 暫緩（尚有 10 筆住戶落選）✅
> · 發佈後住戶端正確：`H3-6` 看到 531／530 各 1,200、自行車 87 免費
>
> **這輪驗不到的**：工作人員的 1,200 元 —— 他被暫緩、沒配到位，而落選列的 `應繳金額` 本就留空（`export/result.js:55`），屬正常。
> 要驗得讓住戶全數配到位（把那 10 筆落選戶的「電腦選號」打開，工作人員才輪得到）。單元測試已蓋（9/9 斷言改 1,200）。
>
> **今天抓到的 bug**：`0013_audit_log.sql` **漏 `grant all ... to service_role`** → 稽核寫不進去，
> 又因 `audit()` 刻意不拋錯而**靜默失敗**（表在、RLS 對、零紀錄）。已補進 migration；
> 線上需另跑 `grant all on public.audit_log to service_role;` 補授權。
>
> **下次從這裡開始**
> 1. 補跑上述 grant → 發佈一次 → 查 `audit_log` 確認 `result.publish` 有留痕
> 2. 階段 6.5「一戶多車」（`S1-993` 一般＋重機、`S1-994` 三台一般）—— 仍未跑過
> 3. 工作人員 1,200 元的真實流程驗證（見上）
> 4. 待評估：把「稽核斷線檢查」加進 keepalive workflow（今天發現的盲點；約 15 分）
>
> **未決的設計題**：重機需相鄰兩格，但志願是一格一格選、且全戶共用一份清單 → 住戶無從得知會配到哪個鄰格。
> 方向：①維持現狀 ②志願改每車一份 ③選位時只提示、不預先決定。**使用者尚未定案。**

### 階段 6 · 抽籤與驗證

20. 種子填 `手動測試-0902` → 按「執行抽籤」
21. 下載「**內部版 CSV**」

**逐項核對**：

| 檢查 | 怎麼看 | 預期 |
|---|---|---|
| 重機相鄰 ★ | CSV 找 `S1-991` 的車位編號（形如 `123、124`），對照結果地圖 | **兩格實體相連**，非隔走道／轉角／跨排 |
| 社宅 | 找 `S1-992` | 車位編號落在 1–7 或 92–104 |
| 工作人員 ★ | 找 `員工-測試員` | 若仍有住戶落選 → 狀態「暫緩」；否則配到一般位、**應繳 0 元** |
| 順序號 | 整份看 | 連續不跳號、無重複 |
| 鎖定位 | 對照鎖定清單（44 格） | 完全沒被配出去 |

22. 再下載「**公開版 CSV**」

**預期**：`員工-測試員` **不在公開版裡**（工作人員公告隱藏）。

### 階段 6.5 · 一戶多車（2026-09-02 追加；最貼近現實的場景）

> 一戶多車是常態，且**志願清單為戶層級、全戶共用**——同一格會被不同車以不同方式取用，
> 是目前最容易出事、也最少被覆蓋的路徑。建議明天併入同一輪抽籤一起驗。

**測試身分**

| 戶號 | 車輛 | 目的 |
|---|---|---|
| `S1-993` | `TEST-004` 一般（第1輛）＋ `TEST-005` **重機**（第2輛） | ★ 混合車種共用志願：同一格被一般車當單格、被重機當起始格 |
| `S1-994` | `TEST-006`／`007`／`008` 皆一般 | 驗 R1→R2→R3 三輪、一戶一位保障 |

**志願建議**：兩戶都填**同一組連號區**（例如 `230、231、232、233、234`），
才看得出「前面的車先拿走後，後面的車拿到什麼」。

**核對重點**

| 檢查 | 預期 |
|---|---|
| `S1-993` 第 1 輛（一般） | R1 拿到第 1 志願的**單格** |
| `S1-993` 第 2 輛（重機）★ | R2 才輪到；需從**剩下的志願**湊相鄰兩格 —— 記錄它拿到哪兩格 |
| 志願被前車消耗後 | 重機是否正確跳過已被自家一般車佔走的格 |
| `S1-994` 三台 | 第 1 輛 R1、第 2 輛 R2、第 3 輛 R3；**同戶不重複佔同一格** |
| 一戶一位保障 | R1 跑完前，沒有任何一戶拿到第二個位子 |

> 這一輪的結果會直接回答那個未決的設計題：住戶對「重機會配到哪個鄰格」到底有多不可預期。

### 階段 7 · 發佈、查詢、還原

23. 按「發佈結果」→ 登出 → 以 `S1-991` + `TEST-001` 登入 `/me`

**預期**：看得到自己的車位編號與應繳金額。

24. 以 admin 登入 → `/dev` → 「**②½ 清除配位結果（保留住戶資料）**」

**預期**：住戶再登入時已無車位，登記資料仍在。

25. **刪除測試資料**：`/dev`「② 清除測試資料」只刪 9xx 戶號 → `S1-991`／`S1-992` 會被清掉；
    `員工-測試員` 不符 9xx 規則，需用下方 SQL 補刪。

---

## 存證（12/1 正式抽籤同此）

抽籤用的 **CSV 快照 ＋ 種子 ＋ 執行時間**三者一起留 —— 缺一個就重跑不出同樣結果。
