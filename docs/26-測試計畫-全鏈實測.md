# 全鏈實測計畫（2026-09-01）

> 原訂 8 月底、已逾期。本輪同時驗收 9/01 新增的三項功能。
> 分工：**A 段自動**（引擎／API，Claude 跑）、**B 段手動**（瀏覽器 UI，使用者跑）。

## 本輪必看的新功能（8/27–9/01 新增，皆未經真實流程）

| # | 功能 | 驗什麼 |
|---|---|---|
| N1 | 重機**實體相鄰**配對 | 配到的兩格真的相連，無隔走道／轉角（原 71 對假相鄰） |
| N2 | `/allocate`「⬇ 載入真實名冊」 | 能載入、且**電話欄為空**（個資最小化） |
| N3 | 工作人員選位限制 | 公益位與無障礙位點不下去 |

---

## A 段：自動（Claude 執行）

| 編號 | 項目 | 判定 |
|---|---|---|
| A1 | 單元測試 196 ＋ `vite build` | ✅ 全綠、build 成功 |
| A2 | 引擎全鏈：真實鎖定格（44）＋410 列名冊 | ✅ 重複配位 0、鎖定位外洩 0、同戶第1輛重複 0 |
| A3 | 重機實體相鄰（N1） | ✅ 18 台（雙格 16＋無障礙單格 2），假相鄰 **0** |
| A4 | 健檢腳本 13 項 | ✅ 全 13 項正確觸發（需處理 8＋參考 5） |
| A5 | API 負面路徑 | ✅ 六支 403；三張敏感表 REST 直讀回 `42501 permission denied` |
| A6 | CSV round-trip ＋ 戶層級旗標 | ✅ 旗標不失真、戶層級不一致 **0 戶** |

**A 段結論（2026-09-01 執行）：全數通過。** 明細：
- 名冊 410 列 → 配出 175、落選 159；剩餘可配 420 格
- 工作人員：配出 0、暫緩 2（因尚有住戶未配到）→ 符合辦法伍二（十二）；公開版正確隱藏 2 筆
- 社宅：配出 11 筆，**無一配到非公益位**
- 落選結構：未填志願 155｜志願全落選 2｜工作人員暫緩 2
  > ⚠️ 155 筆「未填志願」是 mock 名冊特性（僅 4 列有志願），非缺陷。
  > 但它印證了電腦選號預設打勾的必要——場上還剩 420 格卻有人配不到。

## B 段：手動（瀏覽器，使用者執行）

> 網址 `http://localhost:5173/ParkFee/`

| 編號 | 步驟 | 預期 |
|---|---|---|
| B1 | `/me` 以 admin 登入（戶號 `admin`、車號欄填管理員密碼） | 進得去，nav 出現管理入口 |
| B2 | **正案路徑**：Dashboard SQL → Download CSV → `/dev` 匯入 | 頂端顯示「✓ 已載入…」且戶數對得上 |
| B3 | 備案路徑（N2）：`/allocate` 按「⬇ 載入真實名冊」 | 能載入；匯出 CSV 檢查**電話欄為空** |
| B4 | `/allocate` 填種子 → 執行抽籤 | 有結果、順序號連續、種子與時間留存 |
| B5 | 對照結果地圖看重機配位（N1） | 兩格實體相連，非隔走道／轉角 |
| B6 | 以工作人員身分進 `/select`（N3） | 公益位／無障礙位不可點，圖例正確 |
| B7 | 發佈結果 → 以住戶登入 `/me` 查 | 查得到自己的位子；工作人員在公開版隱藏 |
| B8 | 清除配位結果 | 回到未配位狀態，可重跑 |

### B 段回填（測完填這裡，留作驗收紀錄）

> 執行者：＿＿＿　執行日期：＿＿＿　站台：<https://joechiboo.github.io/ParkFee/>
> ⚠️ 正式站台與本機 dev **連同一個 Supabase**，測試資料請用 `S1-99x`／`員工-測試%`，測完清除。

| 編號 | 結果 | 備註（不符預期時寫觀察到什麼） |
|---|---|---|
| B1 admin 登入 | ☐ PASS ☐ FAIL | |
| B2 正案：Dashboard CSV → 匯入 | ☐ PASS ☐ FAIL | 戶數＿＿ |
| B3 備案：一鍵載入（**驗電話欄為空**） | ☐ PASS ☐ FAIL | 戶數＿＿／電話欄：☐空 ☐有值 |
| B4 抽籤執行 | ☐ PASS ☐ FAIL | 種子＿＿＿＿ |
| B5 重機配位實體相鄰 | ☐ PASS ☐ FAIL | 抽查編號＿＿、＿＿ |
| B6 工作人員選位限制 | ☐ PASS ☐ FAIL | |
| B7 發佈 → 住戶查詢 | ☐ PASS ☐ FAIL | |
| B8 清除復原 | ☐ PASS ☐ FAIL | |

**測試資料清除（測完務必執行）**

```sql
delete from public.locked_seat ls
where ls.車位編號 in (
  select trim(unnest(string_to_array(v.車位編號, '、')))
  from public.vehicle v
  where (v.戶號 like 'S1-99%' or v.戶號 like '員工-測試%') and v.車位編號 is not null
);
delete from public.household where 戶號 like 'S1-99%' or 戶號 like '員工-測試%';
```

---

## 存證（12/1 正式抽籤同此）

抽籤用的 **CSV 快照 ＋ 種子 ＋ 執行時間**三者一起留 —— 缺一個就重跑不出同樣結果。
