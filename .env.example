# 複製成 .env 後填入你的 Supabase 專案值（Project Settings → API）。
# .env 已被 .gitignore 排除，不會進 repo。

# Project URL，例：https://abcdefgh.supabase.co
VITE_SUPABASE_URL=

# anon / publishable key —— 公開沒關係，本就會打包進前端、靠 RLS 把關（見 docs/12 §4.2）。
VITE_SUPABASE_ANON_KEY=

# ⚠️ 絕對不要在這裡（或任何 VITE_ 變數、任何 src/ 檔案）放 service_role key。
#    service_role 會繞過 RLS；它只屬於 Edge Function（平台自動注入），放進前端＝全社區資料外洩。
