# ParkFee — 交接／建置規格

> 給接手的 CLI session（如 Claude Code）。讀完這份即可開始建置。
> **唯一事實來源**：`docs/社區地下室停車場管理辦法.pdf`（115/03/21 修訂）。規則衝突一律以辦法為準，不要自行發明流程。

## 1. 任務

把樂菲莊園機車車位的年度作業做成一個**純前端、可重現、可稽核**的工具：
登記 → 分階段配位 → 繳費 → eTag 白名單 → 巡邏稽核。

## 2. 系統概念：三層分離

| 層 | 機制 | 回答的問題 |
|---|---|---|
| 門禁 | eTag | 進得來嗎？（住戶層級） |
| 資格 | 抽籤＋繳費 | 這個位子是不是你的？ |
| 稽核 | 巡邏辨識 | 實際停的車對不對？ |

閘門只管第一層。ParkFee 負責第二、三層，補閘門看不到的部分。

## 3. 與既有 eTag 系統的關係

- **ParkFee 是 system of record**：登記時住戶出示行照、用戶號登記車號，「戶號 ↔ 車號 ↔ 車位」的權威對應建立在 ParkFee。
- **eTag 系統是下游**：只吃 ParkFee 匯出的「車號白名單」放行；閘門認 eTag／車號，不需認戶號。
- **稽核回查**：偵測到的車號 → 回查 ParkFee 取得戶號／車位，不依賴閘門系統是否存戶號。
- **舊資料銜接**：首次上線辦理登記活動即重建乾淨對應；沒登記的舊 eTag 砍掉，對不上戶號的等車主登記時補上。
- ⚠️ **待確認**：現有停車系統的 eTag 資料欄位（有無戶號、能否匯出車號清單）→ 決定首次上線是否需全面重新登記。見 §11。

## 4. 名詞與資料模型

- **戶**：主鍵戶號。一戶可有多輛機車（第 1、2、3… 輛）。
- **機車**：車號唯一、對應一張行照、一個 eTag。車種分一般（≤250CC）與重機（250CC↑，紅／黃牌）。
- **車位**：編號、類型（大／小／無障礙）、是否公益設施、樓層、座標／分區。
  - 一般大 320（公益編號 2,3,6,7,97,98,103,104）
  - 一般小 327（公益編號 1,4,5,92,93,94,95,96,99,100,101,102）
  - 無障礙 8
- **車位空間資料**：車位編號 → 樓層、座標(x,y)／分區、類型、公益標記。來源為 B1–B5 配置圖（待數位化）。供 SVG 互動圖、巡邏定位、選位劃區。
- **登記**：戶 → 機車 → 申請第幾輛車位。登記期每年 11/15–11/30，一戶限登記一次（一次填齊想要的第 1、2… 輛）。
- **配位結果**：戶／車號 → 車位編號 ／ 候補序 ／ 未中。
- **繳費**：抽中後一次繳一年（一般 1,200、重機 3,600）；簽約期限＝公告日＋5 日；公告後 10 日內未辦妥視同放棄、候補遞補。
- **巡邏紀錄**：車位編號 + 偵測車號 + 拍攝時間 + 判定狀態 + 違規累計次數。

## 5. 規則摘要（精確值，依辦法）

### 5.1 分階段配位（選課式）

登記一次填齊需求（每戶第 1、2… 輛、車種、是否身障、是否志願小位）。配位引擎分階段跑，**後階段可用車位 ＝ 前階段剩餘**：

- **階段 0 — 無障礙**：身障優先。登記 ≤ 8 全配；> 8 抽籤配 8，未中併入階段 1。
- **階段 1 — 保障每戶一位**（每戶第 1 輛）：
  - 志願小位者：免抽，依登記順序選小位號碼。
  - 其餘第 1 輛：抽「車位資格」，中籤者依抽出順序從剩餘車位**選號碼**（選課式選位，可用 SVG 圖選靠電梯的位置）。
  - 目標是每個有第 1 輛的戶都拿到一位（655 位 ≈ 655 戶，容量足）。保障的是「**一個位**」，不保證偏好的類型／位置。
- **階段 2 — 剩餘開放多位**（第 2 輛起）：清查階段 1 剩餘 → 開放第 2 輛者競爭。需求 ≤ 剩餘 → 全配選號；> 剩餘 → 公開抽籤定資格＋號碼＋候補序。第 3 輛以上有剩餘再依序辦。

抽籤須可重現：顯示並記錄亂數種子、執行時間、主持／監察委員。

### 5.2 收費（柒）
- 一般機車 100/月 → 抽中一次繳 **1,200/年**
- 250CC↑重機（佔雙機車位）300/月 → **3,600/年**
- 中途退租：依剩餘月份退，不滿一月以一月計（Phase 2）
- 自行車免費、汽車 500/月（皆不在 MVP）

### 5.3 eTag（伍）
- 一車一 eTag，依行照車號；未登記 eTag 禁止入內。
- 工具輸出「已分配且已繳費」車號白名單 → 餵智慧停車系統，名單外一律砍。

### 5.4 罰則（陸）
- 佔他人車位 3,000/日（歸車位所有權人）；輪流停押金 3,000；三次違停／放雜物取消資格；勸導單見附件四（套印 Phase 2）。

## 6. 模組

### 6.1 核心閉環（優先）
登記名冊 → 分階段配位 → 繳費狀態 → eTag 白名單匯出。純函式 + Vue 畫面 + CSV 進出。

### 6.2 車牌辨識＋巡邏稽核（純前端）
照片不上傳、用後即棄。辨識為輔、**人工確認為準**，不可用原始 OCR 結果直接開罰。

兩種用途：
1. **登記掃描帶車號**（簡單，建議第一個落地）：拍行照／正面車牌 → OCR → 帶入車號，人工核對。
2. **巡邏稽核**（較難）：拍停放車輛 → 先選車位編號（SVG 圖點選或手動）→ 辨識 → 比對名冊 → 判定 → 一鍵帶入勸導單。地下室光線／角度差，務必人工確認。

**稽核判定矩陣**：

| 該車位 vs 偵測車號 | 判定 | 動作 |
|---|---|---|
| ＝該位登記車 | 正常 | 無 |
| 有登記、屬別的位子 | 停錯位 | 勸導單；累計 3 次 → 取消資格 |
| 有登記、無資格／未繳 | 佔用他人車位 | 勸導＋3,000/日（歸該位所有權人） |
| 不在名冊 | 未登記／疑外車 | 勸導單「非本社區車輛」＋通報查 eTag |
| 該位空、登記車未停 | 僅紀錄 | 無 |

違規累計 3 次 → 取消資格（陸五），需保存次數。

**v1 做法（最省）**：照片框選車牌區 → deskew → Tesseract.js → 正規化（大寫、補連字號、修 O/0、I/1、B/8）→ 人工確認。自動偵測（onnxruntime-web）列升級，模型 lazy-load。

### 6.3 車位平面圖（SVG）
車位空間資料 → SVG 互動圖。供階段 1／2 選位、巡邏點選定位、停錯標紅。圖資未到位前用「分區＋格號」抽象。

## 7. 技術與檔案結構

- Vue 3 + Vite + Tailwind，部署 GitHub Pages，無後端。
- 資料 CSV 進出；名冊維護在管委會既有試算表。
- 抽籤固定種子亂數，可重現。
- 車牌辨識：Tesseract.js（v1），升級 onnxruntime-web。
- 不存身分證號；照片裝置端處理、不上傳。

```
parkfee/
  docs/社區地下室停車場管理辦法.pdf
  src/
    data/        # CSV 解析、schema 驗證
    lottery/     # 分階段配位（純函式、含 seed）
    fee/         # 費用計算
    lpr/         # 車牌辨識：deskew、OCR、正規化、稽核判定（純函式）
    map/         # 車位 SVG 互動圖（座標 → 渲染、點選）
    export/      # eTag 白名單、結果 CSV、勸導單
    views/       # 登記、配位、結果、巡邏稽核、匯出
    components/
  public/
    models/      # （升級用）車牌偵測模型，lazy-load
```

## 8. CSV Schema

- **匯入 登記名冊**：`戶號, 車號, 車種(一般|重機), 第幾輛, 身障(Y|N), 志願小位(Y|N), 登記時間, 聯絡電話`
- **匯入 車位空間資料**：`車位編號, 樓層, 類型(大|小|無障礙), 公益(Y|N), x, y`（或分區代座標）
- **匯出 配位結果**：`戶號, 車號, 車位編號, 車位類型, 應繳金額, 簽約期限, 狀態(分配|候補|未中)`
- **匯出 eTag 白名單**：`車號, 戶號, 車位編號, 有效起, 有效迄`
- **匯出 巡邏稽核**：`車位編號, 偵測車號, 拍攝時間, 判定狀態, 違規累計`
- **匯出 勸導單（附件四）**：`違規分類, 違規日期時間, 車號, 戶號, 違規事項, 單據編號`

## 9. 演算法規格（純函式，先寫測試）

**分階段配位**
```
input:  registrations[], spaces[], seed
output: { byStage[], assigned[], waitlist[], unassigned[], seed, runAt }
```
- 同輸入＋同 seed → 同結果（mulberry32 之類）。
- 依 §5.1 跑 階段0 → 1 → 2；每階段可用池＝上一階段剩餘。
- 每步記 log，畫面可展開供監察。

**稽核判定**
```
recognizePlate(imageOrRegion) -> { text, confidence }
normalizeTWPlate(raw) -> string
auditPlate(plateText, spaceId, registry) -> { status, 戶號?, 應繳?, 違規累計? }
  // status: 正常 | 停錯位 | 佔用 | 未登記 | 空位
```

## 10. 驗收條件

**核心閉環**
- [ ] 匯入名冊 CSV，schema 錯誤有明確提示。
- [ ] 一鍵跑分階段配位，顯示種子與各階段過程；同種子重跑一致。
- [ ] 階段 1 保障每戶第 1 輛一位；階段 2 正確用剩餘車位、超額產生候補。
- [ ] 配位結果含應繳金額與簽約期限，可匯出 CSV。
- [ ] 可匯出 eTag 車號白名單 CSV。
- [ ] 純前端、可部署 GitHub Pages、不存身分證號。

**車牌辨識／巡邏**
- [ ] 登記畫面可拍照辨識帶入車號，人工可修正。
- [ ] 巡邏：選車位 → 辨識 → 顯示判定＋信心值，人工確認後才落檔。
- [ ] 判定矩陣 5 種情況正確；違規累計達 3 次觸發取消資格標記。
- [ ] 一鍵產生勸導單（附件四）並匯出。
- [ ] 照片裝置端處理、不上傳、處理後不留原圖。

## 11. Open Questions（先用預設、標 TODO，待管委會／廠商確認）

1. **公益設施機車位**是否納入抽籤池？預設：標記 `公益=Y`，暫不納入個人承租。
2. **保障範圍**：階段 1 保障「一個位」還是含偏好類型／位置？預設：只保障一位。
3. **選位順序依據**：志願小位用登記順序、抽中者用抽籤順序 — 是否要全程統一？
4. **重機**：佔雙機車位且「機車位足額」才可，重機保障是否受限、排哪一階段？
5. **重機停雙位「足額」判定門檻**？
6. **未執行期間（去年 10 月起）銜接**：既往不究 or 補登記補費？（曾有追溯爭議）
7. **車位配置圖（B1–B5）數位化**：向建商／管委會取得，先 B1 機車區。
8. **車牌辨識準度門檻**：最低可接受信心值？低於一律轉人工。
9. **eTag 系統資料欄位**：現有系統有無戶號、能否匯出車號清單 → 決定首次是否全面重新登記。
10. 抽籤監察是否需簽核欄位套印？（Phase 2）

## 12. 建議開發順序

1. Vue + Vite + Tailwind 骨架；CSV 匯入＋schema 驗證。
2. 純函式分階段配位模組（先寫測試：固定 seed → 固定結果）。
3. 配位畫面、結果與 eTag 白名單匯出 → 核心閉環可跑。
4. 登記掃描帶車號（低風險 LPR 起步）。
5. 車位 SVG 圖（待圖資）＋選位。
6. 巡邏稽核＋勸導單。

建 UI 時讀 `frontend-design` skill。
